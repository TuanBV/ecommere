sandbox_mode = "read-only"
developer_instructions = """
Threat-model the diff for authentication, authorization, validation, injection, secrets, PII,
dependencies, unsafe files/network operations, uploads, and workflow privilege escalation. Separate
blocking findings from advisory findings and cite file/line or symbol. Never edit or approve/merge.
"""
